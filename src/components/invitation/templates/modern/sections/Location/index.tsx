import { ArrowUpRight } from "lucide-react";
import type { SectionProps } from "../../../../types";
export function Location({ invitation }: SectionProps){const events=invitation.events;if(!events.length)return null;return <section id="details" className="bg-[#f0efeb] px-5 py-18 text-[#0c0e14] sm:px-8 lg:px-12"><div className="mx-auto max-w-[1500px]"><p className="text-[10px] font-bold tracking-[.35em] text-[#765cff] uppercase">Event passes</p><h2 className="mt-3 text-[clamp(3.8rem,10vw,8rem)] font-black leading-[.78] tracking-[-.07em] uppercase">Be there.</h2><div className="mt-8 space-y-4">{events.map((event,index)=>{const day=event.eventDate?new Intl.DateTimeFormat("id-ID",{day:"2-digit"}).format(event.eventDate):"--";const mon=event.eventDate?new Intl.DateTimeFormat("id-ID",{month:"short",year:"numeric"}).format(event.eventDate):"---";const time=event.startTime?(event.endTime?`${event.startTime} — ${event.endTime}`:event.startTime):null;return <article key={event.id} className="grid border border-black/18 bg-white/35 lg:grid-cols-[.72fr_1.28fr]"><div className="p-6 lg:border-r lg:border-black/18"><p className="text-[9px] font-bold uppercase text-black/40">Pass 0{index+1}</p><div className="mt-6 flex items-end gap-4"><span className="text-[clamp(5rem,11vw,9rem)] font-black leading-[.7] text-[#ff5f9f]">{day}</span><div><p className="font-black uppercase">{mon}</p>{time&&<p className="mt-2 text-[10px] font-bold text-[#765cff]">{time} WIB</p>}</div></div></div><div className="p-6 lg:p-8"><p className="text-[9px] font-bold uppercase text-black/40">{event.type.replaceAll("_"," ")}</p><h3 className="mt-5 text-3xl font-black uppercase sm:text-5xl">{event.title}</h3>{event.location&&<p className="mt-5 text-sm font-black uppercase">{event.location}</p>}{event.address&&<p className="mt-2 text-sm leading-7 text-black/52">{event.address}</p>}{event.mapsUrl&&<a href={event.mapsUrl} target="_blank" rel="noopener noreferrer" className="mt-7 inline-flex h-12 items-center gap-3 bg-[#0c0e14] px-5 text-[10px] font-black text-white uppercase">Open maps <ArrowUpRight className="size-4"/></a>}</div></article>})}</div></div></section>}
