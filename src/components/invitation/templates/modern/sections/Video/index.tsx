import { ArrowUpRight } from "lucide-react";
import type { SectionProps } from "../../../../types";
function getEmbedUrl(url:string){const yt=url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/|youtube\.com\/embed\/)([\w-]{11})/);if(yt)return `https://www.youtube.com/embed/${yt[1]}`;const vm=url.match(/vimeo\.com\/(\d+)/);return vm?`https://player.vimeo.com/video/${vm[1]}`:null;}
export function Video({ invitation }: SectionProps){if(!invitation.videoUrl)return null;const embed=getEmbedUrl(invitation.videoUrl);return <section className="bg-[#0e1118] px-4 py-16 text-white sm:px-6 sm:py-24"><div className="mx-auto max-w-6xl"><div className="mb-6 flex items-end justify-between gap-4"><div><p className="text-[10px] tracking-[.35em] uppercase text-[#b7a4ff]">Motion</p><h2 className="mt-2 text-4xl font-black uppercase tracking-[-.05em] sm:text-6xl">Press play.</h2></div></div><div className="overflow-hidden border border-white/10 bg-black">{embed?<div className="aspect-video"><iframe src={embed} title="Video undangan" className="h-full w-full" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/></div>:<a href={invitation.videoUrl} target="_blank" rel="noopener noreferrer" className="flex aspect-video items-center justify-center gap-2 text-xs font-semibold tracking-[.25em] uppercase text-white/80">Watch video <ArrowUpRight className="size-4"/></a>}</div></div></section>}
